// src/utils/mockData.js
// All collections start empty — data is added through the portal UI.
// In production, replace useState initialised from these with real API calls
// to Cloudflare D1 / KV / Workers.

export const mockClients     = []
export const mockStaff       = []
export const mockCommissions = []
export const mockCmsPages    = []
export const mockUsers       = []
